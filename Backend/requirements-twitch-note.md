# Twitch integration note

This initial Twitch OAuth integration reuses existing dependencies already present in `requirements.txt` (notably `httpx` and `fastapi`).
No additional package was added in this iteration.
